# Web3Forms access key is server-only on Vercel: WEB3FORMS_ACCESS_KEY
# Do not add a VITE_ copy — Vite would bake it into the public JS bundle.

# Optional: Google Analytics 4 measurement ID (e.g. G-XXXXXXXXXX)
# Use this to count quote_form / WhatsApp events on the free Vercel plan.
VITE_GA_MEASUREMENT_ID=
